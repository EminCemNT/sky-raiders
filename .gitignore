node_modules/
dist/
*.log
.DS_Store
.vite/

# 真测截图产物（Playwright 生成，不入版本库）
shot_*.png
shots/

# 内部交接文档（AI 会话 handoff，不对外公开）
AGENT_HANDOFF.md

# 损坏 .git 的备份现场（auto-gc 中断打坏对象库时保留，几千个文件，绝不入库）
.git_broken_*/

# QA 临时工作目录（基线对照 .bak、故意改错阈值的变体文件）
qa_yan/
# QA 运行产物（探针输出的临时报告 .txt，不进版本库）
qa_probes/_*.txt
# WorkBuddy 运行数据目录（会话/任务元数据，非项目源码）
.workbuddy/

# 项目运行数据目录（WorkBuddy 会话/自动化状态，不进版本库，勿删）
.workbuddy/

# QA 临时报告文本
qa_probes/_final.txt

# 发布包（构建产物，按 DISTRIBUTION.md 约定不进 git；d89b7fc 恢复对象库时误 add，8529f28 之后移除）
sky-raiders-web.zip
